# Copy to .env and fill in (shell env vars take precedence). .env is git-ignored.

# Cohere — embeddings (embed-v4.0). https://dashboard.cohere.com/api-keys
CO_API_KEY=

# Anthropic — Toponymy region naming. https://console.anthropic.com/
ANTHROPIC_API_KEY=
